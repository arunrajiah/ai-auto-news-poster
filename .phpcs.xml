<?xml version="1.0"?>
<ruleset name="AI Auto News Poster">
    <description>WordPress coding standards for AI Auto News Poster plugin.</description>

    <!-- What to scan -->
    <file>.</file>

    <!-- Exclude third-party directories, test infrastructure, and non-PHP assets -->
    <exclude-pattern>vendor/</exclude-pattern>
    <exclude-pattern>node_modules/</exclude-pattern>
    <exclude-pattern>tests/</exclude-pattern>
    <exclude-pattern>assets/</exclude-pattern>

    <!-- Base standard (Core only — Docs is too strict for initial development) -->
    <rule ref="WordPress-Core"/>
    <rule ref="WordPress-Extra"/>

    <!-- PHP version compatibility -->
    <config name="minimum_supported_wp_version" value="5.0"/>
    <config name="testVersion" value="7.4-"/>

    <!-- Allow short array syntax -->
    <rule ref="Generic.Arrays.DisallowLongArraySyntax.Found">
        <severity>0</severity>
    </rule>

    <!-- Text domain for i18n checks -->
    <rule ref="WordPress.WP.I18n">
        <properties>
            <property name="text_domain" type="array">
                <element value="ai-auto-news-poster"/>
            </property>
        </properties>
    </rule>

    <!-- Allow slightly longer lines (common in admin templates) -->
    <rule ref="Generic.Files.LineLength">
        <properties>
            <property name="lineLimit" value="120"/>
            <property name="absoluteLineLimit" value="0"/>
        </properties>
    </rule>

    <!-- Suppress nonce check warning inside our AJAX handlers — we check manually. -->
    <rule ref="WordPress.Security.NonceVerification.Missing">
        <exclude-pattern>includes/class-admin-settings.php</exclude-pattern>
    </rule>

    <!--
        The main plugin file intentionally contains both the plugin class and the
        activation-redirect helper function. Suppress the mixed-declarations sniff
        and the class-based file-name sniff for this entry-point file only.
    -->
    <rule ref="WordPress.Files.FileName.InvalidClassFileName">
        <exclude-pattern>ai-auto-news-poster.php</exclude-pattern>
    </rule>
    <rule ref="Universal.Files.SeparateFunctionsFromOO.Mixed">
        <exclude-pattern>ai-auto-news-poster.php</exclude-pattern>
    </rule>

    <!-- Inline-comment punctuation and doc-comment style rules are informational only. -->
    <rule ref="Squiz.Commenting.InlineComment.InvalidEndChar">
        <severity>0</severity>
    </rule>
    <rule ref="Squiz.Commenting.FunctionComment.ParamCommentFullStop">
        <severity>0</severity>
    </rule>
    <rule ref="Squiz.Commenting.FunctionComment.MissingParamTag">
        <severity>0</severity>
    </rule>
    <rule ref="Squiz.Commenting.ClassComment.Missing">
        <severity>0</severity>
    </rule>
    <rule ref="Squiz.Commenting.VariableComment.Missing">
        <severity>0</severity>
    </rule>
    <rule ref="Squiz.Commenting.FunctionComment.IncorrectTypeHint">
        <severity>0</severity>
    </rule>
    <rule ref="WordPress.Files.FileName.InvalidClassFileName">
        <severity>0</severity>
    </rule>
</ruleset>
